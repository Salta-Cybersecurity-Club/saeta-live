# Contribuir

- PRs chicos y con tests. Corre `pytest -q` antes de pushear.
- Formato: `black` con config default.
- Reviews: al menos un OK de @tbarrios-salta, @mdelgado-noa o @rodo-f.
- No commitear `.env`, dumps de `data/dumps/` ni credenciales.
